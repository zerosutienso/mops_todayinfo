[package]
name = "mops_todayinfo"
version = "0.1.0"
edition = "2024"

[dependencies]
headless_chrome = "1.0"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["cookies", "json"] }
scraper = "0.19"
# 🎯 核心新增：CSV 編碼儲存套件
csv = "1.3"
regex = "1.10"
# 🎯 核心新增：日期時間處理套件
chrono = "0.4"